@model IEnumerable<Hotel_reservas.Models.Promocion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-hover">
    <tr>
        <th>
         promocion
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descuento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.promocion1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descuento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_promocion }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_promocion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_promocion })
        </td>
    </tr>
}

</table>
